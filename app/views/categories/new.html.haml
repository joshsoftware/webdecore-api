.container-fluid
  .row
    .col-md-4.offset-md-4
      %h1 Add Primary Category
      = bootstrap_form_with scope: :category, url: categories_path,local: true do |form|
        %p
          = form.text_field :category_name, required: true
        %p
          = form.text_field :category_description, required: true
        %p
          = form.file_field :picture, id: "picture", onchange: "picture_file(this)", required: true
        %p
          = form.submit
      = button_to 'Cancel', categories_path, method: "get", class: "btn btn-success", :style => "float: left"

:javascript
  function picture_file(element){
    var fileName = document.getElementById("picture").files[0].name;
    var nextSibling = element.nextElementSibling;
    nextSibling.innerText = fileName;
  }
